import { ScrollArea } from "@/components/ui/scroll-area";

export function RightSidebar() {
  return (
    <div className="h-full border-r">
      {/* Contract/File Navigation */}
      <ScrollArea className="h-full">
        {/* Add file tree/navigation here */}
      </ScrollArea>
    </div>
  );
}
